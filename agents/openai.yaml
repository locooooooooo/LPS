interface:
  display_name: "PM Orchestration System"
  short_description: "PM dispatch, worker roles, and version-plan control"
  default_prompt: "Use $pm-orchestration-system to run PM dispatch, long-worker goal mode, short-worker day-close, and version-plan orchestration."

policy:
  allow_implicit_invocation: false
